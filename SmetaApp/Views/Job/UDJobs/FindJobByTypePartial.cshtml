@model IEnumerable<SmetaApp.Models.Job>

<div class="result-space">
@foreach (var j in Model)
{
    <div onclick="$('#find-type').val($(this).text());" class="find-result" >@j.Type</div>
}
</div>

<script>
    if ("@Model.Any()"== "False")
        $(".result-space").hide();
</script>